
## UV Commands


```bash
# init repo
uv init

# create env
uv venv

# list packages in env
uv pip list

# install package editable
uv pip install -e .

# install dev dependency
uv add --dev pytest

# create lock file
uv lock
```
